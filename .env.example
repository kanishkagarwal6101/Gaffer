# Gaffer environment variables — copy to .env and fill in. Never commit .env.

# Google AI Studio (ai.google.dev) — primary app LLM (Gemini Flash). Free, no card.
GEMINI_API_KEY=

# Groq console — optional LLM fallback (Llama 3.3 70B). Free.
GROQ_API_KEY=
