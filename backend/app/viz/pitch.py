"""mplsoccer renderers -> PNG (plan sections 2, 5).

Football-correct pitch plots rendered server-side to PNG: shot maps (with xG),
pass networks, and radar charts for player comparison. Returns image paths the
API serves statically (or base64 for v1).

No logic yet — stub for milestone M2.
"""
